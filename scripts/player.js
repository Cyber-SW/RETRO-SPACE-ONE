class Player {
	constructor() {
		this.width = 100
		this.height = 140
		this.x = 0
		this.y = 1040 - this.height
        this.color = 
		this.score = 0
	}

	draw() {
        if ()
		image(game.shipOrange, this.x, this.y, this.width, this.height)

        if ()
        image(game.shipGreen, this.x, this.y, this.width, this.height)

        if ()
        image(game.shipRed, this.x, this.y, this.width, this.height)
	}
}

